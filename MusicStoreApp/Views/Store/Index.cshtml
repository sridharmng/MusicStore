@model  IEnumerable< MusicStoreApp.Models.Genre>

@{
    ViewBag.Title = "Store";
}

<h2>Browse Genres</h2>

<h2>select from @Model.Count() genres</h2>
<ul>
    @foreach (var genre in Model)
{
    <li>      @genre.Name</li>
}

</ul>
